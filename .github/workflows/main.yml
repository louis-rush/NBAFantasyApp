name: Run Python Script with Selenium

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests selenium beautifulsoup4

      # Install Google Chrome
      - name: Install Google Chrome
        run: |
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      # Install ChromeDriver for Chrome 130.x
      - name: Install ChromeDriver for Chrome 130.x
        run: |
          sudo apt-get install -y wget unzip
          # Manually specify ChromeDriver version for Chrome 130.x
          wget -N https://chromedriver.storage.googleapis.com/130.0.6723.69/chromedriver_linux64.zip
          unzip chromedriver_linux64.zip
          sudo mv -f chromedriver /usr/local/bin/chromedriver
          sudo chmod +x /usr/local/bin/chromedriver

      # Run the Python script
      - name: Run the Python script
        run: |
          python scraper.py
